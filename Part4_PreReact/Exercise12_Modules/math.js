// Xuất theo tên (Named Export)
export const sum = (a, b) => a + b;

export const subtract = (a, b) => a - b;

// Xuất mặc định (Default Export) - Ví dụ hằng số PI
const PI = 3.14159;
export default PI;
